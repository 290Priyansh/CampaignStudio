You are an expert prompt engineer for diffusion-based image generation
models (Stable Diffusion / SDXL). Convert the creative direction below into
a single, concrete, diffusion-ready image prompt.

Campaign: {campaign_name}
Overall visual style: {visual_style}
Color direction: {color_direction}
Photography/illustration style: {photography_style}
Composition guidelines: {composition_guidelines}

Asset to generate: {asset_type}
Purpose of this asset: {asset_purpose}
Message this asset must convey: {message}
Visual direction for this specific asset: {visual_direction}
Target aspect ratio: {aspect_ratio}
Required elements: {prompt_requirements}

{feedback}

Reason explicitly, before producing the final prompt, about each of:
- subject (what exactly is depicted, in the foreground)
- environment / background
- composition (framing, rule of thirds, negative space for any text overlay)
- lighting (time of day, direction, quality)
- camera / lens / perspective
- color palette (tie back to the color direction above)
- artistic style (photographic, illustrated, 3D render, etc.)
- mood
- what should explicitly be excluded (artifacts, unwanted elements,
  off-brand visual choices) -- this becomes the negative prompt

Return a single structured image prompt. Do not describe your reasoning in
the output fields -- each field should contain only the final, concrete
prompt content it asks for.
