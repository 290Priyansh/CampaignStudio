# --- Core config / validation -----------------------------------------------
setuptools>=69.0.0,<70.0.0   # crewai's telemetry module imports pkg_resources at runtime,
                     # which was removed in setuptools>=70.0.0. Pinned to <70.0.0.
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- LLM orchestration --------------------------------------------------------
crewai==0.83.0
langchain==0.3.7
langchain-ollama==0.2.0
langchain-core==0.3.15
langgraph==0.2.45          # used if ORCHESTRATION_FRAMEWORK=langgraph
ollama==0.3.3

# --- Image generation (local/open-source only) --------------------------------
diffusers==0.31.0
transformers==4.46.2
accelerate==1.1.1
safetensors==0.4.5
torch==2.5.1
torchvision==0.20.1
pillow==11.0.0

# --- Evaluation ----------------------------------------------------------------
open-clip-torch==2.29.0    # local CLIP for semantic (image<->prompt) scoring

# --- RAG (optional, only used if RAG_ENABLED=true) ------------------------------
chromadb==0.5.18

# --- UI --------------------------------------------------------------------------
streamlit==1.40.1

# --- Testing -----------------------------------------------------------------
pytest==8.3.3
pytest-mock==3.14.0
